{
  "name": "@vault-lookup/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b && node scripts/copy-assets.js",
    "dev": "node --watch --experimental-strip-types src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@fastify/static": "^10.1.2",
    "@vault-lookup/shared": "*",
    "cheerio": "^1.0.0",
    "fastify": "^5.2.0"
  }
}
